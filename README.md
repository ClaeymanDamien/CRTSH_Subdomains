# CRTSH_Subdomains

```
❯ python3 main.py -h
usage: main.py [-h] -d DOMAIN [-o OUTPUT]

Check if new subdomains were created

optional arguments:
  -h, --help            show this help message and exit
  -d DOMAIN, --domain DOMAIN
                        Domain to be analyzed
  -o OUTPUT, --output OUTPUT
                        Path folder to save results
``
